{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cfi-fed
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "cfi-fed.labels" . | nindent 4 }}
  {{- if .Values.ingress.rewrite }}
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: cfi-fed-tls
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: {{ if .Values.ingress.rewrite }}/registry(/|$)(.*){{ else }}/registry{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-registry
                port:
                  number: {{ .Values.registry.port }}
          - path: {{ if .Values.ingress.rewrite }}/coordinator(/|$)(.*){{ else }}/coordinator{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-coordinator
                port:
                  number: {{ .Values.coordinator.port }}
          - path: {{ if .Values.ingress.rewrite }}/aggregator(/|$)(.*){{ else }}/aggregator{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-aggregator
                port:
                  number: {{ .Values.aggregator.port }}
          {{- if .Values.replayHooks.enabled }}
          - path: {{ if .Values.ingress.rewrite }}/replay(/|$)(.*){{ else }}/replay{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-replay-mock
                port:
                  number: {{ .Values.replayHooks.mock.port }}
          - path: {{ if .Values.ingress.rewrite }}/agentrx(/|$)(.*){{ else }}/agentrx{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-agentrx
                port:
                  number: {{ .Values.replayHooks.agentrx.port }}
          - path: {{ if .Values.ingress.rewrite }}/causalflow(/|$)(.*){{ else }}/causalflow{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-causalflow
                port:
                  number: {{ .Values.replayHooks.causalflow.port }}
          - path: {{ if .Values.ingress.rewrite }}/tau(/|$)(.*){{ else }}/tau{{ end }}
            pathType: {{ if .Values.ingress.rewrite }}ImplementationSpecific{{ else }}Prefix{{ end }}
            backend:
              service:
                name: cfi-tau
                port:
                  number: {{ .Values.replayHooks.tau.port }}
          {{- end }}
{{- end }}
